# Mock ServiceNow Dockerfile
FROM python:3.11-slim

WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn pydantic
COPY mock_snow /app/mock_snow

ENV MOCK_SNOW_HOST=0.0.0.0
ENV MOCK_SNOW_PORT=8001

EXPOSE 8001
CMD ["python", "mock_snow/main.py"]
